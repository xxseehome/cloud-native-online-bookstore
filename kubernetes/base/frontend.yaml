apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookstore-frontend
  labels:
    app.kubernetes.io/name: bookstore-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: bookstore-frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bookstore-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/xxseehome/cloud-native-online-bookstore-frontend:IMAGE_TAG
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
              add:
                - NET_BIND_SERVICE
            readOnlyRootFilesystem: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
---
apiVersion: v1
kind: Service
metadata:
  name: bookstore-frontend
spec:
  selector:
    app.kubernetes.io/name: bookstore-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
