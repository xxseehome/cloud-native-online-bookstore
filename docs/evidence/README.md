# Redacted acceptance screenshots

Store final screenshots here only after the corresponding run or console page
is complete. Filenames should identify the evidence item, for example
`ci-security-gates.png` or `staging-resilience.png`.

Before committing an image, confirm that it contains no credentials, tokens,
private keys, account numbers, email addresses, or unrelated personal data.
The links in `../acceptance-evidence.md` are the authoritative index.
